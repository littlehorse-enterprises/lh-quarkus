artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: LittleHorse Quarkus
metadata:
  keywords:
    - littlehorse
  categories:
    - "workflow"
    - "workflow engine"
    - "microservices"
  status: "preview"
